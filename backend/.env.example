# PoC / simulação: SQLite (zero setup, sem banco externo)
DATABASE_URL=sqlite:///./farol.db
# Alternativa com PostgreSQL:
# DATABASE_URL=postgresql+psycopg://farol:farol@localhost:5432/farol

SECRET_KEY=dev-secret
DEBUG=true
CORS_ORIGINS=["http://localhost:5173"]
