VITE_API_URL=
# Em desenvolvimento local, deixe vazio para usar o proxy do Vite
# Em produção/Docker, aponte para o backend:
# VITE_API_URL=http://localhost:8000

# Modo de demonstração (apenas frontend - não altera backend)
# Quando true: bypassa login, usa usuário demo local, não envia credenciais ao backend
VITE_DEMO_MODE=false
